// PRISMA SCHEMA para integración con Supabase
// Opcional: puedes usar Prisma como ORM sobre Supabase
// Instala: npm install prisma @prisma/client
// Inicializa: npx prisma init
// Genera cliente: npx prisma generate
// Ejecuta migraciones: npx prisma migrate dev --name initial

// datasource db {
//   provider = "postgresql"
//   url      = env("DATABASE_URL")
// }
//
// generator client {
//   provider = "prisma-client-js"
// }
//
// model User {
//   id        String   @id @default(uuid())
//   email     String   @unique
//   name      String
//   role      Role     @default(USER)
//   avatarUrl String?  @map("avatar_url")
//   createdAt DateTime @default(now()) @map("created_at")
//
//   @@map("users")
// }
//
// enum Role {
//   ADMIN
//   USER
// }
//
// model Maestro {
//   id         String     @id @default(uuid())
//   nombre     String
//   saldo      Decimal    @default(0) @db.Decimal(10, 2)
//   creadoPor  String     @map("creado_por")
//   createdAt  DateTime   @default(now()) @map("created_at")
//   movements  Movement[]
//
//   @@map("maestros")
// }
//
// model Movement {
//   id            String   @id @default(uuid())
//   maestroId     String   @map("maestro_id")
//   maestro       Maestro  @relation(fields: [maestroId], references: [id], onDelete: Cascade)
//   maestroNombre String   @map("maestro_nombre")
//   tipo          TipoMovimiento
//   cantidad      Decimal  @db.Decimal(10, 2)
//   responsable   String
//   fecha         DateTime @default(now())
//
//   @@map("movements")
// }
//
// enum TipoMovimiento {
//   ENTRADA
//   SALIDA
// }

// NOTA: Prisma es opcional. Puedes usar directamente el cliente de Supabase.
